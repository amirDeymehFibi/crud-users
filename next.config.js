const nextConfig = {
  experimental: {
    outputStandalone: true,
    output: "standalone",
  },
  reactStrictMode: true,
  images: {
    unoptimized: true,
  },
  output: "standalone",
};

module.exports = nextConfig;
